## 2.2. 消去法的概念 47

**圖 2.6：範例 1 的列圖與行圖：無解。**

平行線不會相交。

第一行 \[3]
第二行 \[-6]

行不組合產生 **b** = \[11]。

**範例 2：失敗，具有無限多解。將 **b** 改變為 (1, 11) 到 (1, 3)。**

x − 2y = 1
3x − 6y = 3

減去 3 倍
eqn. 1 從 eqn. 2

x − 2y = 1
0y = 0

仍然只有
一條。

每個人都滿足 0y = 0。實際上只有一個方程式 x − 2y = 1。未知數 y 是“自由的”。因此，x 由 x = 1 + 2y 決定。

在行圖中，平行線恰好變成同一條線。每條線上的一個點都滿足兩個方程式。我們有一個整列的解，如圖 2.7 所示。在列圖中，**b** = (1, 3) 恰好與第一列相同。因此，我們可以選擇 x = 1 且 y = 0。我們也可以選擇 x = 0 且 y = −½；列 2 乘以 −½ 等於 **b**。每個 (x, y) 都能解決列問題。

**失敗** 對於 n 個方程式，我們不能得到 n 個樞紐。

消去法導致一個方程式 0 ≠ 0 (無解) 或 0 = 0 (無限多解)

成功來自於 n 個樞紐。我們可能需要交換 n 個方程式。

消去法可以走錯路，但這可以被修正。假設第一個樞紐位置包含零。我們可以交換它與一個包含非零元素的方程式：

**範例 3：暫時失敗（樞紐位置為零）。行交換產生兩個樞紐：**

0x + 2y = 4
3x − 2y = 5

交換兩個方程式

3x − 2y = 5
2y = 4

置換

這個新系統已經是標準形式。最後一個方程式給了 y = 2，然後第一個方程式給了 x = 3。行圖顯示兩條相交線（兩條相交線）。列圖顯示列是正常的（列向量相同）。樞紐位置 2 也是正常的。交換行是必要的。

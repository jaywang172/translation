## 1.2. 長度和點積 21

**圖：** 顯示 cos α = v₁/||v|| 和 sin α = v₂/||v||。同樣地 cos β = w₁/||w|| 和 sin β = w₂/||w||。角度 β 是 β - α。代入三角函數公式 cos β cos α + sin β sin α for cos(β - α) 以得到 cos θ = v ⋅ w / (||v|| ||w||)。

**23**  圖示顯示 cos α = v₁/||v|| 和 sin α = v₂/||v||。同樣地 cos β = w₁/||w|| 和 sin β = w₂/||w||。角度 θ 是 β - α。代入三角函數公式 cos β cos α + sin β sin α for cos(β - α) 以得到 cos θ = v ⋅ w / (||v|| ||w||)。

**24**  施瓦茨不等式的一行證明 |**u** ⋅ **v**| ≤ 1 對於單位向量：

|**u** ⋅ **v**| ≤ |u₁||v₁| + |u₂||v₂| = √(u₁² + u₂²) √(v₁² + v₂²) / 2 = 1 + 1 / 2 = 1。

將 (u₁, u₂) = (.6, .8) 和 (v₁, v₂) = (.8, .6) 代入整條線並求 cos θ。

**25**  為什麼 |cos θ| 永遠不會大於 1？

**26**  如果 w = (x, y) 在 xy 平面上，且 **w** ⋅ **v** = x + 2y = 5。哪個 w 是最短的？

**27**  (推薦題) 如果 ||**v**|| = 5 且 ||**w**|| = 3，那麼 ||**v** - **w**|| 的最小值和最大值是什麼？ **v** ⋅ **w** 的最小值和最大值是什麼？

**挑戰問題**

**28**  能否在 xy 平面中找到三個向量 **u**、**v** 和 **w**，使得 **u** ⋅ **v** > 0 且 **v** ⋅ **w** > 0 且 **u** ⋅ **w** < 0？我不知道 xy 平面中有多少個向量可以有負的點積。（這四個向量在平面中一定不可能。）

**29**  選擇任何數字加總到 x + y + z = 0。找出向量 **v** = (x, y, z) 和向量 **w** = (z, x, y)。挑戰問題：解釋為什麼 **v** ⋅ **w** / (||**v**|| ||**w**||) 總是 -½。

**30**  如何證明 √(xyz ≤ (x + y + z) / 3) (幾何平均數 ≤ 算術平均數)？

**31**  找出四個具有所有成分等於 ±½ 的單位向量。

**32**  使用 v = randn(3, 1) 在 MATLAB 中建立一個隨機單位向量 **u** = **v** / ||**v**||。使用 w = randn(3, 1) 建立另一個隨機單位向量。計算 **u** 和 **w** 之間的角度 θ：θ = acos(**u** ⋅ **w** / (||**u**|| ||**w**||))。重複此過程 1000 次並繪製直方圖，顯示 θ 的分佈。如果 μ 是平均值，則 μ ≈ 2π/3。

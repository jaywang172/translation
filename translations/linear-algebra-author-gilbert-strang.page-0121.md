## 118 章 2. 解線性方程式

**問題 22-24 關於分解式 $PA = LU$ 與 $A = L_1P_1U_1$ (請檢查它們)**

22 找出 $PA = LU$ 分解式 (並檢查它們) 對於：

$\qquad A = \begin{bmatrix} 0 & 1 & 1 \\ 1 & 0 & 1 \\ 2 & 3 & 4 \end{bmatrix}$ 和 $\qquad A = \begin{bmatrix} 1 & 2 & 0 \\ 2 & 4 & 1 \\ 1 & 1 & 1 \end{bmatrix}$

23 找出一個 4 x 4 置換矩陣 (稱之為 $A$) 需要 3 次列交換才能達到消去法的終點。對於這個矩陣，找出它的因式分解 $P$、$L$ 和 $U$。

24 將下列矩陣分解成 $PA = LU$。將因式分解也寫成 $A = L_1P_1U_1$ (保持將列 3 與列 1 交換 3 次所得到的 1 減去矩陣)。

$\qquad A = \begin{bmatrix} 0 & 1 & 2 \\ 0 & 3 & 8 \\ 2 & 1 & 1 \end{bmatrix}$

25 擴展 2.6 節中的滑動法則，以產生將矩陣 $PA$ 分解成 $LU$ 的法則。

26 證明單位矩陣不能被表示為三個交換 (或五個) 的乘積。它可以是兩個交換的乘積 (或四個)。

27 (a) 選擇 $E_{21}$ 以移除第一欄中的 3。然後將 $E_{21}A E_{1}$ 相乘以移除兩個 3。

$\qquad A = \begin{bmatrix} 1 & 3 & 0 \\ 3 & 11 & 4 \\ 0 & 4 & 9 \end{bmatrix}$ 正在朝向 $\qquad D = \begin{bmatrix} 1 & 0 & 0 \\ 0 & 2 & 0 \\ 0 & 0 & 1 \end{bmatrix}$

(b) 選擇 $E_{32}$ 以移除第二欄中的 4。將 $E_{32}E_{21}A E_{1}$ 轉換成 $D$。求出 $L$ 使 $L D L^T = A$。

28 如果一個 4 x 4 矩陣的每一列包含數字 0、1、2、3 以某種順序，這個矩陣是否可以是對稱的？

29 證明不論如何重新排列行和重新排列列，都可以轉置一個典型的矩陣。(注意對角線元素。)

接下來三個問題是關於應用恆等式 $(Ax)^T y = x^T(Ay)$。

30 電線連接波士頓、芝加哥和西雅圖。這些城市處於電壓 $x_B$、$x_C$、$x_S$。單位電阻連接這些城市。電流在城市之間是 $y_B$、$y_C$、$y_S$。

$\qquad y = Ax$ 是 $\qquad \begin{bmatrix} y_B \\ y_C \\ y_S \end{bmatrix} = \begin{bmatrix} -1 & 1 & 0 \\ 0 & -1 & 1 \\ 1 & 0 & -1 \end{bmatrix} \begin{bmatrix} x_B \\ x_C \\ x_S \end{bmatrix}$

(a) 找出流出三個城市的總電流 $A^T y$。
(b) 驗證 $(Ax)^T y$ 等於 $x^T(Ay)$—六個項相加。

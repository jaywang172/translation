## 54 第 2 章：解線性方程組

**18** 構建一個 3x3 範例，其左側有 9 個不同的係數，且在消元過程中行 2 和行 3 變成零。對於您的系統，在 b = (1, 10, 100) 和 b = (0, 0, 0) 時，有多少個解？

**19** 哪個數字 *q* 使系統奇異且右側 *g* 產生無限多個解？求使解為 *z* = 1 的解。

```
x + 4y - 2z = 1
x + 7y - 6z = 6
3y + qz = t
```

**20** 三個平面可能無法有交集點，即使沒有任何平面是平行的。如果 A 的行列式為 0，則該系統是奇異的，並且是前兩行的線性組合。求一個無法與 x + y + z = 0 和 x - 2y = 1 一起求解的第三個方程式。

**21** 求兩個系統的樞軸和解 (4x = b 和 Kx = b)：

```
2x + y = 0      2x - y = 0
x + 2y + z = 0   -x + 2y - z = 0
y + 2z + t = 0   -y + 2z - t = 0
z + 2t = 5      -z + 2t = 5
```

**22** 如果您延伸問題 21，遵循 1, 2, -1 模式或 -1, 2, -1 模式，則第五個樞軸是什麼？K 是我最喜歡的矩陣。

**23** 如果消元導致 x + y = 1 且 2y = 3，求三個可能的原始問題。

**24** 對於哪個兩個數字 *a*，消元將在 A = [a a²] 上失敗以產生樞軸？

**25** 對於哪個三個數字 *a*，消元將無法產生樞軸？

```
A = [ a  2  3 ]
    [ a  a  4 ]
    [ a  a  a ]
```

**26** 尋找一個矩陣，其行和為 8 和 4，列和為 2 和 *s*：

```
Matrix = [ a  b ]
         [ c  d ]
```

其中 a + b = 4, a + c = 2, c + d = 8, b + d = *s*

如果四個方程式可解，則 *ifs* = ...。然後找出兩個不同的矩陣，它們具有正確的行和列和。額外信用：用消元法寫出 4x4 系統 Ax = b，其中 x = (a, b, c, d)，並將其化為三角形式。

**27** 消元在現實世界中通常使用矩陣 U 和替換解決這個“水位”問題：

我們正在尋找由以下方程式定義的靜水壓力：

```
x + 3y + 2z = 3
2x + 7y + z = 8
3x + 5y + 2z = 9
```

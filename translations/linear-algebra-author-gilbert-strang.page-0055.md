## 52 第 2 章：解線性方程式

**3** 應從方程式 1 減去方程式 2 的多少倍？

```
2x - 4y = 6
-x + 5y = 0
```

在完成消去法步驟後，解這個三角系統。如果右側變為 (-6, 0)，新的解是什麼？

**4** 應從方程式 1 減去方程式 2 的多少倍以消除 *x*？

```
ax + by = f
cx + dy = g
```

第一個樞軸點（假設非零）。消去法產生什麼公式來計算第二個樞軸點？當 *ad* = *bc* 時，第二個樞軸點缺失時，系統是怎樣的？

**5** 選擇一個右側，使其沒有解，另一個右側使其有無限多個解。這兩種解各有哪些？

```
3x + 2y = 10
6x + 4y = __
```

奇異系統

**6** 選擇一個係數 *b* 使這個系統為奇異系統。然後選擇一個右側 *g* 使其可解。在那個奇異情況下找到兩個解。

```
2x + by = 16
4x + 8y = g
```

**7** 對於哪些數字 *a*，消去法會崩潰？(1) 永久性 (2) 暫時性？

```
ax + 3y = -3
4x + 6y = 6
```

求解 *x* 和 *y*，在通過行交換修復暫時崩潰的情況下。

**8** 對於哪些三個數字 *k*，消去法會崩潰？哪一個崩潰是由行交換固定的？在每種情況下，解的數量是 0 或 ∞？

```
kx + 3y = 6
3x + ky = -6
```

**9** 什麼測試 *b₁* 和 *b₂* 決定這兩個方程式是否有解？它們會有多少個解？繪製 *b* = (1, 2) 和 (1, 0) 的列圖。

```
3x - 2y = b₁
6x - 4y = b₂
```

**10** 在 *xy* 平面上繪製線 *x* + *y* = 5 和 *x* + 2*y* = 6 的方程式，以及從消去法中得到的方程式 *y* = ____。線 5*x* - 4*y* = *c* 將穿過這兩個方程式的解，如果 *c* = ____。

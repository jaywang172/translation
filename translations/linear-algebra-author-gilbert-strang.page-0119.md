## 116 章 2. 解線性方程組

**3** (a) 矩陣 $((AB)^{-1})^T$ 是由 $(A^{-1})^T$ 和 $(B^{-1})^T$ 的什麼順序組成？
   (b) 如果 $U$ 是上三角矩陣，則 $U^{-1}$ 的轉置是 ______ 三角矩陣。

**4** 證明 $A^T A^T = 0$ 是不可能的（除非 $A = 0$ 零矩陣）。

**5** (a) 行向量 $x^T$ 乘以列向量 $y$ 產生什麼數值？
   $x^T y = \begin{bmatrix} 0 & 1 & 1 \end{bmatrix} \begin{bmatrix} 1 \\ 2 \\ 3 \end{bmatrix} = 1$

   (b) 此行向量 $x^T A$ 等於 ______ 乘以列向量 $y = (0, 1, 0)$.
   (c) 此行向量 $x^T = [0 \ 1 \ ]$ 乘以列向量 $Ay = $ ______

**6** 區塊矩陣 $M = \begin{bmatrix} A & B \\ C & D \end{bmatrix}$ 的轉置是 $M^T = $ ______。 測試一個例子。
   在什麼條件下，$A, B, C, D$ 是區塊矩陣對稱的？

**7** 真或假：
   (a) 區塊矩陣 $\begin{bmatrix} A & 0 \\ 0 & B \end{bmatrix}$ 自動對稱。
   (b) 如果 $A$ 和 $B$ 是對稱矩陣，則它們的乘積 $AB$ 是對稱的。
   (c) 如果 $A$ 不是對稱矩陣，則 $A - A^T$ 不是對稱的。
   (d) 如果 $A, B, C$ 是對稱矩陣，則 $ABC$ 等於 $CBA$。

**關於 8-15 題：置換矩陣。**

**8** 為什麼有 $n!$ 個 $n$ 階置換矩陣？

**9** 如果 $P_1$ 和 $P_2$ 是置換矩陣，則 $P_1 P_2$ 仍然是 $I$ 的行排列。給出例子，使得 $P_1 P_2 \neq P_2 P_1$ 且 $P_3 P_4 = P_4 P_3$。

**10** 有 12 個“偶”置換 (1, 2, 3, 4)，其中兩個是沒有交換的，而其他 10 個有交換。
   兩個例子是 (1, 2, 3, 4) 和 (4, 3, 2, 1) 具有一個交換。
   列出其餘的。不要寫成 4x4 矩陣，只需寫出數字。

**11** 哪個置換使得 $PA$ 上三角？哪個置換使得 $P_1 A P_2$ 下三角？將 $A$ 的右側乘以 $P$ 交換兩行。
   $A = \begin{bmatrix} 0 & 0 & 6 \\ 1 & 2 & 3 \\ 0 & 4 & 5 \end{bmatrix}$

**12** 說明為什麼 $x^T y$ 的點積等於 $Px^T Py$ 的點積。
   從 $x^T y = (Px)^T (Py)$ 得到 $x^T y$ 證明 $x$ 和 $y$ 都是 $P$ 的向量。
   如果 $x = (1, 2, 3)$ 和 $y = (1, 4, 2)$ 選擇 $P$ 使得 $Px = (1, 0, 0)$ 和 $Py = (0, 1, 0)$。
   (a) 找到 $A$ 和 $B$ 使得置換矩陣 $P$ 滿足 $PA = A$ 和 $PB = B$。
   (b) 找到 3x3 置換矩陣，將矩陣 $x$ 轉換為 $y$。

3.6. 四個子空間的維度 $\quad$ 193

**19** 依照問題 18 的方法，將 $A$ 化為階梯形並觀察零列。$b$ 欄告訴你對列進行了哪些組合：

(a) $\begin{bmatrix} 1 & 2 & b_1 \\ 3 & 4 & b_2 \\ 4 & 6 & b_3 \end{bmatrix}$ (b) $\begin{bmatrix} 1 & 2 & b_1 \\ 2 & 3 & b_2 \\ 2 & 4 & b_3 \\ 2 & 5 & b_4 \end{bmatrix}$

在消去之後，從 $b$ 欄讀出左零空間中的 $m-r$ 個基向量。這些 $y$ 是使得結果為零列的列組合。

**20** (a) 驗證 $Ax = 0$ 的解與列正交：

$A = \begin{bmatrix} 1 & 0 & 0 \\ 2 & 1 & 0 \\ 3 & 4 & 1 \end{bmatrix} \begin{bmatrix} 4 & 2 & 0 & 1 \\ 0 & 0 & 1 & 3 \\ 0 & 0 & 0 & 0 \end{bmatrix} = ER.$

(b) $A^Ty = 0$ 有多少個獨立解？為什麼 $y^T$ 是 $E^{-1}$ 的最後一列？

**21** 假設 $A$ 是兩個秩（rank）為 1 的矩陣之和：$A = uv^T + wz^T$。

(a) 哪些向量能生成 $A$ 的行空間？
(b) 哪些向量能生成 $A$ 的列空間？
(c) 若 \_\_\_\_\_\_ 或 \_\_\_\_\_\_，則秩小於 2。
(d) 若 $u = z = (1, 0, 0)$ 且 $v = w = (0, 0, 1)$，請計算 $A$ 及其秩。

**22** 構造一個 $A = uv^T + wz^T$，使其行空間的基為 $(1, 2, 4), (2, 2, 1)$，且其列空間的基為 $(1, 0), (1, 1)$。將 $A$ 寫成 $(3 \times 2)$ 矩陣乘以 $(2 \times 2)$ 矩陣的形式。

**23** 在不進行矩陣乘法的情況下，找出 $A$ 的列空間與行空間的基：

$A = \begin{bmatrix} 1 & 2 \\ 4 & 5 \\ 2 & 7 \end{bmatrix} \begin{bmatrix} 3 & 0 & 3 \\ 1 & 1 & 2 \end{bmatrix}.$

從這些維度中，你如何知道 $A$ 不可能是可逆的？

**24** （重要）當 $d$ 位於四個子空間中的哪一個時，$A^Ty = d$ 是可解的？當 \_\_\_\_\_\_ 僅包含零向量時，解 $y$ 是唯一的。

**25** 判斷正誤（請提供理由或反例）：

(a) $A$ 與 $A^T$ 具有相同數量的主元（pivots）。
(b) $A$ 與 $A^T$ 具有相同的左零空間。
(c) 若列空間等於行空間，則 $A^T = A$。
(d) 若 $A^T = -A$，則 $A$ 的列空間等於行空間。

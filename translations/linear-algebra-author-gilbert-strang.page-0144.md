## 3.2 零空間：解 Ax = 0

**(a)**  $A = \begin{bmatrix} -1 & 3 & 5 \\ -2 & 6 & 10 \end{bmatrix}$

**(b)**  $B = \begin{bmatrix} -1 & 3 & 5 \\ -2 & 6 & 7 \end{bmatrix}$

**6** 對於相同的 A 和 B，找出 Ax = 0 和 Bx = 0 的特殊解的數量。對於一個 m x n 矩陣，樞軸變數的數量與自由變數的數量之間有何關係？

**7** 在問題 5 中，描述 A 和 B 的零空間，並以兩種方式表達這些方程式：平面或線的方程式，以及所有滿足這些方程式的向量 x 的組合。

**8** 將問題 5 中的 echelon 形式 U 簡化為 R。對於每個 R，在樞軸列和樞軸欄中畫一個方框。

**問題 9-17 關於自由變數和樞軸變數。**

**9** 真或假（附上理由，如果為假，請舉例說明）：

**(a)** 一個方陣沒有自由變數。
**(b)** 一個可逆矩陣沒有自由變數。
**(c)** 一個 m x n 矩陣最多有 n 個樞軸變數。
**(d)** 一個 m x n 矩陣最多有 m 個樞軸變數。

**10** 構造 3 x 3 矩陣 A 以滿足以下要求（如果可能）：

**(a)** A 沒有零條目，但 U = I。
**(b)** A 有零條目，但 R = I。
**(c)** A 有零條目，但 R = U。
**(d)** A = U = 2R。

**11** 在一個 4 x 7 echelon 矩陣 U 中，放置盡可能多的 1，樞軸欄是：

**(a)** 2, 4, 5
**(b)** 1, 3, 6, 7
**(c)** 4 和 6。

**12** 在一個 4 x 8 簡化 echelon 矩陣 R 中，放置盡可能多的 1，自由欄是：

**(a)** 2, 4, 5, 6
**(b)** 1, 3, 6, 7, 8。

**13** 假設一個 4 x 5 矩陣的第四欄全部為零。那麼 $x_4$ 肯定是一個特殊解的變數。

**14** 假設一個 3 x 5 矩陣的第一和最後欄是相同的（零向量）。那麼 ______ 是一個自由變數。找出此變數的特殊解。

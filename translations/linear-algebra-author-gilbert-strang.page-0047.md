## 44 章 2. 解線性方程式

**33 乘以 A 是一個「線性轉換」。那些重要的詞彙意指：**

如果 *w* 是 *u* 和 *v* 的組合，那麼 *Aw* 是 *Au* 和 *Av* 的相同組合。

這就是「線性性」*Aw = cAu + dAv* 給予線性代數名稱的原因。

問題：如果 *u =*  \[ 0 ]
\[ 1 ]  和 *v =*  \[ 0 ]
\[ 1 ]  那麼 *Au* 和 *Av* 是 *A* 的哪些欄？

組合 *w = cu + dv*。如果 *w =*  \[ 5 ]
\[ 7 ]  那麼 *Aw* 如何與 *Au* 和 *Av* 相關聯？

**34 從四個方程式 -x<sub>i+1</sub> + 2x<sub>i</sub> - x<sub>i-1</sub> = i (對於 i = 1, 2, 3, 4) 且 x<sub>0</sub> = x<sub>5</sub> = 0 開始。將這些方程式寫成矩陣形式 *Ax = b*。你能解出它們來求 x<sub>1</sub>, x<sub>2</sub>, x<sub>3</sub>, x<sub>4</sub> 嗎？**

**35 一個 9x9 的數獨矩陣 *S* 包含在每一列和每一行中的數字 1,...,9，並且在每一個 3x3 的區塊中。對於全為 1 的向量 *x =* (1,...,1)，什麼是 *Sx*？**

一個更好的問題是：哪些列交換會產生另一個數獨矩陣？

哪些區塊交換會產生另一個數獨矩陣？

第 2.7 節將會檢視所有可能的列重新排列（重新排序）。我可以看到 6 個順序對於前 3 列，全部產生數獨矩陣。還有 6 個順序對於接下來的 3 列，以及最後 3 列。還有 6 個區塊的排列？

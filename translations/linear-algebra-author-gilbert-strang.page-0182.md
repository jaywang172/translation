## 3.5. 獨立性、基底與維度 179

**4** 如果在問題 3 中 *a*, *d*, *f* 均不為零，證明唯一的解 *Ux* = **0** 是 *x* = **0**。則上三角矩陣 *U* 具有獨立的欄向量。

**5** 決定下列向量的獨立性或相依性：

(a) 向量 (1, 3, 2) 和 (2, 1, 3) 和 (3, 2, 1)
(b) 向量 (1, -3, 2) 和 (2, 1, -3) 和 (-3, 2, 1)

**6** 從 *U* 中選擇三個獨立的欄向量。然後做另外兩個選擇。對 *A* 亦然。

```
U =  [ 2  3  4  1 ]      A = [ 2  3  4  1 ]
     [ 0  6  7  0 ]           [ 0  6  7  0 ]
     [ 0  0  0  9 ]           [ 0  0  0  9 ]
     [ 0  0  0  0 ]           [ 4  6  8  2 ]
```

**7** 如果 **w₁**, **w₂**, **w₃** 是獨立向量，證明其差向量 **v₁** = **w₂** - **w₃** 和 **v₂** = **w₁** - **w₃** 和 **v₃** = **w₁** - **w₂** 是相依的。找出給定 **v** 的線性組合，使得 **v** = *c₁* **v₁** + *c₂* **v₂** + *c₃* **v₃**。 *A* 是哪個矩陣？

**8** 如果 **w₁**, **w₂**, **w₃** 是獨立向量，證明其和 **v₁** = **w₂** + **w₃** 和 **v₂** = **w₁** + **w₃** 和 **v₃** = **w₁** + **w₂** 是獨立的。（寫出 *c₁* **v₁** + *c₂* **v₂** + *c₃* **v₃** = **0** 並找出 *c* 的值，以證明它們為零。）

**9** 假設 **v₁**, **v₂**, **v₃**, **v₄** 是 **R³** 中的向量。

(a) 這些向量是相依的，因為…
(b) 這兩個向量 **v₁** 和 **v₂** 將會相依，如果…
(c) 向量 **v₁** 和 (**0**, **0**, **0**) 是相依的，因為…

**10** 找出兩個獨立向量於平面 *x* + 2*y* - 3*z* = **0** 在 **R⁴** 中。然後找出三個獨立向量。為什麼不能找到？這個平面是哪個空間的零空間？

**問題 11-15 是關於由向量組所張出的空間。取線性組合。**

**11** 描述由下列向量張出的 **R³** 的子空間（是平面還是 **R³**？）：

(a) 向量 (1, 1, 1) 和 (1, -1, 1)
(b) 向量 (1, 0, 1) 和 (0, 1, 1)
(c) 向量 (1, 1, 1) 和 (1, 1, 1) 和 (0, 0, 0)
(d) 向量在 **R³** 中，其分量之和為零。

**12** 找出所有在 **R⁴** 中滿足 *x* + *y* + *z* + *w* = 0 的向量 **x** = ( *x₁*, *x₂*, *x₃*, *x₄* )。 這些向量張出一個什麼空間？ 找出這個空間的基底。

**13** 向量 **v₁** 和 **v₂** 在 **R²** 中是線性獨立的。證明 **v₁** + **v₂** 和 **v₁** - **v₂** 也是線性獨立的。

## 2.1. 向 量與 線性 方程

43

**25** 如果你將 4 x 4 全 1 矩陣 A = ones(4,4) 與 向量 v = ones(4,1) 相乘，結果會是 A*v？ (計算不需提供。) 如果你將矩陣 B = eye(4) + ones(4,4) 與 向量 w = zeros(4,1) + 2*ones(4,1) 相乘，結果會是 B*w？

**問題 26-28 複習 行列式與向量在 2, 3, 和 4 維空間中的圖像。**

**26** 繪製方程式 x - 2y = 0 與 x + y = 6 的行向量與列向量圖像。

**27** 對於兩個線性方程式，具有三個未知數 x, y, z，行向量圖像將會顯示 (2 或 3) 條線或平面 (在 2 或 3) 維空間中。 解通常位於一個 ______。

**28** 對於四個線性方程式，具有兩個未知數 x 和 y，行向量圖像顯示 ______。 列向量圖像在 ______ 維空間中。 除非向量在右側是組合，否則方程式沒有解。

**29** 假設向量 u₀ = (1, 0)。 再次且再次地使用相同的 “馬可夫矩陣” A =  [ .8  .3 ]
                                                                 [ .2  .7 ]。 下三個向量是 u₁, u₂, u₃:

u₁ =  [ .8  .3 ] [ 0 ] = [ .8 ]
       [ .2  .7 ] [ 0 ] = [ .2 ]

u₂ = A u₁ = ______

u₃ = A u₂ = ______

你對所有向量 u₀, u₁, u₂, u₃ 有什麼觀察？

**挑戰問題**

**30** 繼續問題 29，從 u₀ = (1, 0) 開始，並從 v₀ = (0, 1) 開始到 v₇。 你會注意到 u₇ 和 v₇ 有什麼？ 這裡提供兩個 MATLAB 程式碼，使用 while 和 for。 你可以使用其他程式語言：

u = [1; 0]; A = [ .8  .3 ; .2  .7 ];
x = v = [0; 0];
k = 7;
while size(x, 2) < k;
    A = A;
    x = [x; A*x];
end
plot(x)

v = [0; 1]; A = [ .8  .3 ; .2  .7 ];
x = v; k = 7;
for j = 1:7
    A = A;
    x = [x; A*x];
end
plot(x)

**31**  uₙ 和 vₙ 如果是逼近一個穩定狀態向量 v₈。 猜測這個向量是什麼？

**32** 考慮一個 2 x 2 矩陣 A = [ 1 1 ]
                                  [ 1 0 ]。 找到 A 的所有 eigenvectors 與 eigenvalues。 驗證你的答案是否滿足 A v = λv。

**33** 讓我們來建立一個馬可夫矩陣，其中包含 3 個狀態。 假設有 3 個類別的顧客：忠誠顧客 (L)，偶爾顧客 (O)，和新顧客 (N)。 顧客從一個類別轉移到另一個類別的機率如下： 忠誠顧客保持忠誠顧客的機率為 0.8，轉移到偶爾顧客的機率為 0.1，轉移到新顧客的機率為 0.1。 偶爾顧客保持偶爾顧客的機率為 0.6，轉移到忠誠顧客的機率為 0.3，轉移到新顧客的機率為 0.1。 新顧客轉移到偶爾顧客的機率為 0.7，轉移到忠誠顧客的機率為 0.3。

(a) 建立這個馬可夫矩陣 A。

(b) 如果初始顧客分佈為 30% 忠誠顧客，30% 偶爾顧客，40% 新顧客，那麼在 10 個時間步驟後，顧客分佈會是什麼？

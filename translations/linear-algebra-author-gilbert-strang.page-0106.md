## 2.6. 消去法：因式分解：A = LU 103

**3** (從 3x3 到 3x3) 前向消去法將 Ax = b 轉換為一個三角形式 Ux = c：

```
x + y + z = 5   x + y + z = 5   x + y + z = 5
x + 2y + 3z = 7   y + 2z = 2   y + 2z = 2
x + 3y + 6z = 11   2y + 5z = 6   z = 2
```

方程式 z = 2 在 Ux = c 中來自原始的 x + 3y + 6z = 11。
Ax = b 通過減去 L₃₁ = ( ) 倍方程式 1 和 L₃₂ = ( ) 倍方程式 2。
為了恢復 [ 1 1 5 ] 和 [ 0 1 2 ] 和 [ 0 0 2 ] 在 U 和 c 中，從最終 [ 1 1 5 ] 和 [ 0 1 2 ] 和 [ 0 0 2 ] 中。

Row 3 of [A] = (L₃₁ Row 1 + L₃₂ Row 2 + L₃₃ Row 3) of [U c].

在矩陣表示中，這就是乘法形式 LSA = LU 和 b = Lc。

**4** 什麼是 3x3 三角系統 Le = b 和 Ux = c，來自問題 3？
檢查 (5, 2, 2) 是否解決第二個方程？

**5** 什麼矩陣 E 將 A 放入三角形式 EA = U？ 將 E⁻¹ = L 用於將 A 分解為 LU：

```
A = [ 2 1 0 ]
    [ 0 4 2 ]
    [ 6 3 5 ]
```

**6** 什麼兩個消去矩陣 E₂₁ 和 E₃₂ 將 A 放入上三角形式 E₃₂E₂₁A = U？ 將 E₃₂ 和 E₂₁ 用於將 A 分解為 LUU = E₂₁⁻¹E₃₂⁻¹。

```
A = [ 1 1 0 ]
    [ 2 4 5 ]
    [ 0 4 0 ]
```

**7** 什麼三個消去矩陣 E₂₁, E₃₁, E₃₂ 將 A 放入其上三角形式 E₃₂E₃₁E₂₁A = U？ 將 E₂₁, E₃₁, E₃₂ 用於將 A 分解為 L 的 U。

```
A = [ 1 0 1 ]
    [ 2 2 2 ]
    [ 3 4 5 ]
```

**8** 假設 A 已經具有 1 位於對角線上。 那麼 E = I！

```
A = L = [ 1 0 0 ]
          [ 0 1 0 ]
          [ 0 0 1 ]
```

消去矩陣 E₂₁, E₃₁, E₃₂ 包含 (b - then - c)。

(a) 利用 E₂₁ = E₃₂ 找到 Row 2 和 Row 3 的乘數。
(b) 利用 L = E₂₁⁻¹E₃₁⁻¹E₃₂⁻¹ 找到 L 矩陣的元素 (在對角線下方)。

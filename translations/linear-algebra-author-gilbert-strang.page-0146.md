## 3.2. A 的零空間：解 Ax = 0

**29** 隨機選取的 3x3 矩陣 A 的簡化形式 R 幾乎可以肯定是…？如果隨機 A 是 4x4 矩陣，情況又如何？

**30** 舉例說明以下三個陳述通常是錯誤的：
   (a) A 和 Aᵀ 具有相同的零空間。
   (b) A 和 Aᵀ 具有相同的自由變數。
   (c) 如果 R 是 rref(A) 的簡化形式，則 R 是 rref(Aᵀ) 的簡化形式。

**31** 如果 A 的零空間由所有 x = (2, 1, 0, 1, 0) 的倍數組成，則 R 中有多少個主變數？

**32** 對於特殊解集 Rx = 0，請回溯這些 N 的欄，找到簡化矩陣 R 的非零列：

   N =  
   ```
   [ 2  3 ]
   [ 1  0 ]
   [ 0  1 ]
   ```
   N = 
   ```
   [ 0 ]
   [ 1 ]
   ```
   N = 
   ```
   [ ]
   [ ]
   [ ]
   ```
   (空 3x1)

**33** (a) 有哪些 2x2 簡化階梯矩陣 R 僅包含 0 和 1？
   (b) 有哪些 2x2 矩陣包含 0 和 1？它們都是簡化階梯矩陣 R 嗎？

**34** 說明為什麼 A 和 -A 總是具有相同的簡化形式 R。

**挑戰問題**

**35** 如果 A 是 4x4 且可逆，描述 4x8 矩陣 B = [A A] 的零空間中的所有向量。

**36** 零空間 N(C) 與空間 N(A) 和 N(B) 有何關聯，如果 C = [A B]？

**37** 基爾霍夫定律指出，在每個節點處，電流流入 = 電流流出。此網路有六個電流 y₁,…,y₆（箭頭表示正向，每個 yᵢ 可以是正數或負數）。找出描述基爾霍夫定律在四個節點處的四個方程式 A y = 0。找出零空間 A 中的三個特殊解。

   ![網路圖，標示節點 1, 2, 3, 4，以及電流 y1, y2, y3, y4, y5, y6](圖表無法顯示，描述為網路圖，標示節點 1, 2, 3, 4，以及電流 y1, y2, y3, y4, y5, y6)

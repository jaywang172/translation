## 90 第 2 章：解線性方程組

**11** (a) 尋找可逆矩陣 *A* 和 *B* 使得 *A* + *B* 不可逆。
   (b) 尋找奇異矩陣 *A* 和 *B* 使得 *A* + *B* 可逆。

**12** 如果積 *C* = *AB* 是可逆的（*A* 和 *B* 都是方陣），則 *A* 本身是可逆的。
   求 *A*<sup>-1</sup> 的公式，其中包含 *C*<sup>-1</sup> 和 *B*。

**13** 如果積 *M* = *ABC* 由三個方陣組成是可逆的，則 *A* 本身是可逆的。
   （假設 *A* 和 *C*。）求 *A*<sup>-1</sup> 的公式，其中包含 *M*<sup>-1</sup>、*A* 和 *C*。

**14** 如果你將第 1 列加到第 2 列以得到 *B*，如何由 *B*<sup>-1</sup> 得到 *A*<sup>-1</sup>？

   注意，矩陣 *B* =  [ 1 0 ]
                      [ 1 1 ] [ A ] 的逆矩陣是…

**15** 證明一個矩陣若包含一列全為零，則它沒有逆矩陣。

**16** 將 [ a b ] 乘以 [ -b a ]。 這是什麼？ 每個矩陣的逆矩陣是什麼，如果 *ad* ≠ *bc*？
      [ c d ]       [ d c ]

**17** (a) 什麼 3 by 3 矩陣 *E* 具有相同的效果，即從第 3 列減去第 1 列，再從第 2 列減去第 3 列？
   (b) 什麼單一矩陣 *L* 具有相同的效果，即將第 2 列加到第 3 列，將第 1 列加到第 3 列，然後再將第 1 列加到第 2 列？

**18** 如果 *B* 是 *A*<sup>2</sup> 的逆矩陣，證明 *AB* 是 *A* 的逆矩陣。

**19** 找出數字 *a* 和 *b*，使得下列 2 x 6 矩陣是 5 * eye(4) - ones(4,4) 的逆矩陣：

   [ -4 -1 -1 -1 -1 ]   [ a b b b ]
   [ -1  4 -1 -1 -1 ] = [ b a b b ]
   [ -1 -1  4 -1 -1 ]   [ b b a b ]
   [ -1 -1 -1  4 -1 ]   [ b b b a ]

   *a* 和 *b* 在 6 * eye(5) - ones(5,5)？

**20** 證明 *A* = 4 * eye(4) - ones(4,4) 是不可逆的：將 *A* 乘以 4 * ones(4,1)。

**21** 有十六個 2 by 2 矩陣，其元素不是 1's 和 0's。有多少個是可逆的？

**問題 22 - 28 關於使用高斯-喬丹方法計算 *A*<sup>-1</sup>。**

**22** 改變 *A* 如下，並執行以下 4 個基本運算：

   [ 1 3 1 ]       [ 1 3 1 ]
   [ A ] = [ 2 6 2 ] 變成 [ 4 9 0 ]
   [ 1 3 1 ]       [ 1 4 1 ]

**23** 假設 *A* 包含文本 *B* 和文本 *I*，其中 *I* 是 3 x 3 的單位矩陣，並且 *A* 包含以下步驟：

   [ 1 1 1 ]
   [ A ] = [ 2 2 2 ]
   [ 0 0 1 ]

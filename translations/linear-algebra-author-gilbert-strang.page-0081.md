## 78 章 2. 解線性方程

**18** 你會用什麼詞語來描述這些類型的矩陣？在每個類別中給出一個 3x3 的例子。哪個矩陣屬於所有四個類別？

(a) $a_{ij} = 0$ if $i \neq j$
(b) $a_{ij} = 0$ if $i < j$
(c) $a_{ij} = a_{ji}$
(d) $a_{ij} = a_{1j}$

**19** 假設矩陣 $A$ 的元素是 $a_{ij}$。假設矩陣中沒有零，那麼：

(a) 第一個主元是什麼？
(b) 需要從第 1 行減去哪個倍數才能從第 3 行中減去？
(c) 減去後取代 $a_{32}$ 的新元素是什麼？
(d) 第二個主元是什麼？

**問題 20-24 涉及 $A$ 的冪。**

**20** 計算 $A^2$、$A^3$、$A^4$ 以及 $A\mathbf{v}$、$A^2\mathbf{v}$、$A^3\mathbf{v}$、$A^4\mathbf{v}$，其中

$A = \begin{bmatrix} 0 & 2 & 0 & 0 \\ 0 & 0 & 2 & 0 \\ 0 & 0 & 0 & 2 \\ 0 & 0 & 0 & 0 \end{bmatrix}$  and  $\mathbf{v} = \begin{bmatrix} x \\ y \\ z \\ t \end{bmatrix}$

**21** 找出所有冪 $A^2$、$A^3$、... 以及 $AB(AB)^2$、...，其中

$A = \begin{bmatrix} -.5 & .5 \\ .5 & .5 \end{bmatrix}$  and  $B = \begin{bmatrix} 1 & 0 \\ -1 & 0 \end{bmatrix}$.

**22** 透過試錯法找出兩個非零的 2x2 矩陣 $A$ 和 $B$，使得

$A^2 = -I$  $BC = 0$  $DE = -ED$ (不允許 $DE = 0$)

**23** (a) 找出一個非零矩陣 $A$，使得 $A^2 = 0$。
(b) 找出一個矩陣，使得 $A^2 \neq 0$ 但 $A^3 = 0$。

**24** 透過實驗，設 $n = 2$ 且 $n = 3$，預測 $A^n$ 對於這些矩陣：

$A_1 = \begin{bmatrix} 2 & 1 \\ 0 & 1 \end{bmatrix}$  and  $A_2 = \begin{bmatrix} 1 & 1 \\ 1 & 1 \end{bmatrix}$  and  $A_3 = \begin{bmatrix} a & b \\ 0 & 0 \end{bmatrix}$.

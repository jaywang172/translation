## 2.7. 轉置與置換 117

**14** 若 P 具有反對角線上的 1，從 (1,n) 到 (n,1)，描述 PAP。注意 P = Pᵀ。

**15** 所有交換矩陣是對稱的：Pᵀ = P。然後 PᵀP = I 成為 P² = I。其他置換矩陣可能對稱也可能不對稱。

(a) 若 P 將第 1 列傳送到第 n 列，則 Pᵀ 將第 row ______ 傳送到 row ______。
當 Pᵀ = P 時，置換交換成成對且不重疊。

(b) 找到一個 4 x 4 的例子，其中 Pᵀ = P 將所有四列移動。

問題 16-21 關於對稱矩陣及其分解。

**16** 若 A = Aᵀ 且 B = Bᵀ，則下列哪些矩陣肯定是對稱的？

(a) A² - B²
(b) (A + B)(A - B)
(c) ABA
(d) ABAB

**17** 找到 2 個滿足 A = Aᵀ 且具有以下性質的 2 x 2 對稱矩陣 A：

(a) A 是不可逆的。
(b) A 是可逆的，但無法分解成 LU（需要交換列）。
(c) A 可以分解成 LDLᵀ，但不能分解成 LLTᵀ（因為 D 為負數）。

**18** (a) 若 A = Aᵀ 且 A 是 5 x 5 的，則可以獨立選擇 A 的多少個元素？
(b) 選擇 D 和 L（不包含對角線元素）的數量是否與選擇 LLDLTᵀ 中的相同？
(c) 若 A 是斜對稱的（Aᵀ = -A），則需要選擇多少個元素？

**19** 假設 R 是矩形的 (m x n) 且 A 是對稱的 (m x m)。

(a) 轉置 RᵀAR 以顯示其對稱性。這個矩陣是什麼形狀？
(b) 證明 RᵀR 沒有負數在對角線上。

**20** 分解這些對稱矩陣為 A = LDLᵀ。樞軸矩陣 D 是對角矩陣：

A =  
$\begin{bmatrix} 3 & 1 \\ 1 & 2 \end{bmatrix}$
和
A = 
$\begin{bmatrix} 1 & b \\ b & 1 \end{bmatrix}$
和
A = 
$\begin{bmatrix} 2 & -1 & 0 \\ -1 & 2 & -1 \\ 0 & -1 & 2 \end{bmatrix}$

**21** 在消去清除第一列下方的元素後，找到在右下角出現的 2 x 2 對稱矩陣：

從 A = 
$\begin{bmatrix} 2 & 4 & 8 \\ 4 & 3 & 9 \\ 8 & 9 & 0 \end{bmatrix}$
開始，且 A = 
$\begin{bmatrix} 1 & b & c \\ 0 & d & e \\ 0 & e & f \end{bmatrix}$

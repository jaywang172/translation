## 2.2. 消去法的概念 55

**28** 建立一個 MATLAB 指令 `A(2, :) = ...`，用於將新第 2 列減去現有第 1 列的 3 倍，如果矩陣 A 已經已知。

**挑戰問題**

**29** 實驗性地找出 MATLAB 函數 `[L, U] = lu(rand(3))` 產生的平均第 1 列、第 2 列和第 3 列的尺寸。平均尺寸 `abs(U(1, 1))` 應大於 $\frac{1}{2}$，因為 `lu` 函數會選擇第 1 列中可用的最大可用樞軸元素。這裡 A = `rand(3)` 隨機生成介於 0 和 1 之間的數值。

**30** 如果矩陣 A 的最後一個角落元素 A(5, 5) = 11，且矩陣 U 的最後一個樞軸元素 U(5, 5) = 4，那麼矩陣 A 的 A(5, 5) 應該是什麼值才能使 A 變成奇異矩陣？

**31** 假設消去法將 A 轉換為 U，且沒有進行列交換。那麼行 j 的 U 是否是行 i 的線性組合？如果 Ax = 0，是 U x = 0 嗎？如果 Ax = b，是 U x = b 嗎？如果 A 從下三角矩陣開始，那麼 U 是什麼形式的上三角矩陣？

**32** 從 100 個方程式 Ax = 0 對 100 個未知數 x = (x1,...,x100) 開始。假設消去法將第 100 個方程式簡化為 0 = 0，因此該系統是“奇異的”。

(a) 消去法產生了行的線性組合。因此，這個奇異系統具有奇異屬性：某個 100 行的線性組合為 ______。

(b) 奇異系統 Ax = 0 具有無限多個解。這意味著某些 100 列的線性組合是 ______。

(c) 發明一個 100 x 100 的奇異矩陣，其中沒有任何零元素。

(d) 對於你的矩陣，用行向量和列向量描述 Ax = 0。不需要用文字描述 100 維空間。

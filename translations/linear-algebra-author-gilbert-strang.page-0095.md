## 92 第 2 章：解線性方程組

**32** 這個矩陣是一個值得注意的逆矩陣。首先用 [A] 進行消元法求出 A⁻¹，然後將其乘開以驗證。

```
Invert A =  [ 1  -1   1 ]
            [ 0   1  -1 ]
            [ 0   0   1 ]
```

並求解 Ax = (1, 1, 1)。

**33** 假設矩陣 P 和 Q 具有相同的行，只是順序不同。它們是“置換矩陣”。證明 P - Q 是奇異的，通過求解 (P - Q)x = 0。

**34** 找到並驗證（假設存在）這些分塊矩陣的逆矩陣：

```
[ A  0 ]
[ C  D ]
```

**35** 一個 4 x 4 矩陣 A 是否可逆，如果它的每個元素都包含數字 0, 1, 2, 3？如果 A 的每個元素都包含 0, 1, 2, -3 呢？

**36** 在習題 2.5 C 的例題中，三角形帕斯卡矩陣 L 具有一個逆矩陣，其對角線為“交替的 1”。檢查這個三角矩陣 L⁻¹ 是否為 DLD，其中 D 是對角線矩陣，D 具有對角線元素 1, -1, 1, -1。然後 L D L D = I，那麼 L D 的逆矩陣是什麼？

**37** 希爾伯特矩陣具有 Hᵢⱼ = 1/(i + j - 1)。使用 MATLAB 找到 6 x 6 希爾伯特矩陣的逆矩陣 invhilb(6)。然後要求 MATLAB 計算 inv(hilb(6))。它們可能不同嗎？計算機誤差是什麼？

**38** (a) 使用 inv(P) 來反轉 MATLAB 的 4 x 4 對稱矩陣 P = pascal(4)。
    (b) 創建帕斯卡矩陣的下三角矩陣 L = abs(pascal(4)) 並測試 P = LLᵀ。

**39** 如果 A = ones(4) 且 b = rand(4,1)，MATLAB 如何告訴你 Ax = b 沒有解？對於 A = ones(4,1) 的特殊情況，Ax = b 的解通過 A\b 找到。

## 挑戰問題

**40** (推薦) A 是一個 4 x 4 矩陣，其對角線元素為 1，對角線上的元素為 -a, -b, -c。找到 A 的逆矩陣。

**41** 假設 E₁, E₂, E₃, E₄ 是 4 x 4 的單位矩陣，除了 E₁ 的第一列，E₂ 的第二列，E₃ 的第三列，E₄ 的第四列，它們都包含一個向量 x = (a, b, c, d)ᵀ。證明 E₁E₂E₃E₄ = I。在什麼條件下，這些矩陣的乘積會產生一個置換矩陣？

**42**  E₁ = E₂ = E₃ = 單位矩陣且所有其他元素為零。證明 (E₁ + E₂ + E₃) 的逆矩陣不存在。

## 3.4. 求解 Ax = b 的完整解

**167**

**32** 求矩陣 A 的 LU 分解以及 Ax = b 的完整解：

```
A =  [ 1  3  1 ]
     [ 1  2  3 ]
     [ 2  4  6 ]
     [ 1  1  5 ]
```

且

```
b = [ 1 ]
    [ 3 ]
    [ 6 ]
    [ 5 ]
```

**33** Ax = [1/3] 的完整解為 x = [1] + c[0] 。求 A。
          [0]           [0]   [1]

**挑戰問題**

**34** 假設你已知一個 3x4 矩陣 A 只有向量 s = (2, 3, 1, 0) 作為 Ax = 0 的唯一特殊解。

(a) A 的秩是多少？以及 Ax = 0 的完整解是什麼？
(b) A 的精簡行階梯形式 R 是什麼？
(c) 你如何知道對於所有 b，Ax = b 都可以被解出來？

**35** 假設 K 是一個 9x9 的第二差分矩陣 (對角線上為 2，對角線上下為 -1)。求解方程 Kx = b = (10, ..., 10)。如果你將 x₁, ..., x₉ 在 x 軸上繪製，我認為這九個點會落在一個拋物線上。

**36** 假設 Ax = b 和 Cx = b 有相同的（完整）解，對於所有 b 而言。是否 A = C？

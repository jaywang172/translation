3.5. 獨立性、基底與維度 181

22 假設 $S$ 是 $\mathbf{R}^6$ 的一個 5 維子空間。判斷正誤（若為誤請舉例說明）：
(a) $S$ 的每一個基底都可以透過增加一個向量而被擴充為 $\mathbf{R}^6$ 的一個基底。
(b) $\mathbf{R}^6$ 的每一個基底都可以透過移除一個向量而被簡化為 $S$ 的一個基底。

23 $U$ 是由 $A$ 減去第 1 列至第 3 列而得：
$$A = \begin{bmatrix} 1 & 3 & 2 \\ 0 & 1 & 1 \\ 1 & 3 & 2 \end{bmatrix} \text{ 以及 } U = \begin{bmatrix} 1 & 3 & 2 \\ 0 & 1 & 1 \\ 0 & 0 & 0 \end{bmatrix}.$$
找出這兩個行空間（column spaces）的基底。找出這兩個列空間（row spaces）的基底。找出這兩個零空間（nullspaces）的基底。哪些空間在消去法中保持不變？

24 判斷正誤（請給出充分理由）：
(a) 若一個矩陣的行向量（columns）線性相依，則其列向量（rows）也線性相依。
(b) $2 \times 2$ 矩陣的行空間與其列空間相同。
(c) $2 \times 2$ 矩陣的行空間與其列空間具有相同的維度。
(d) 矩陣的行向量是該行空間的一個基底。

25 對於哪些數值 $c$ 與 $d$，這些矩陣的秩（rank）為 2？
$$A = \begin{bmatrix} 1 & 2 & 5 & 0 & 5 \\ 0 & 0 & c & 2 & 2 \\ 0 & 0 & 0 & d & 2 \end{bmatrix} \text{ 以及 } B = \begin{bmatrix} c & d \\ d & c \end{bmatrix}.$$

問題 26–30 關於其「向量」為矩陣的空間。

26 找出以下每個 $3 \times 3$ 矩陣子空間的基底（以及維度）：
(a) 所有對角矩陣。
(b) 所有對稱矩陣 ($A^T = A$)。
(c) 所有反對稱矩陣 ($A^T = -A$)。

27 構造六個線性獨立的 $3 \times 3$ 行階梯矩陣（echelon matrices）$U_1, \dots, U_6$。

28 找出所有行之和為零（columns add to zero）的 $2 \times 3$ 矩陣空間的基底。找出其列之和亦為零（rows also add to zero）的子空間基底。

29 由以下項目的所有線性組合所張成（span）的 $3 \times 3$ 矩陣子空間是什麼：
(a) 可逆矩陣？
(b) 秩為 1 的矩陣？
(c) 單位矩陣？

30 找出所有零空間（nullspace）包含 $(2, 1, 1)$ 的 $2 \times 3$ 矩陣空間的基底。

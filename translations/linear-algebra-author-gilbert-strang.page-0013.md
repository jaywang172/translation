10
第 1 章 向量導論

問題 20–25 處理三維空間中的 u, v, w (請參閱圖 1.5b)。

20 在圖 1.5b 中找到 $\frac{1}{3}u + \frac{1}{3}v + \frac{1}{3}w$ 和 $\frac{1}{2}u + \frac{1}{2}w$。挑戰問題：在 $c, d, e$ 受到什麼限制下，$cu + dv + ew$ 的組合會填滿虛線三角形？為了留在三角形內，一個要求是 $c \ge 0, d \ge 0, e \ge 0$。

21 虛線三角形的三個邊是 $v - u$、$w - v$ 和 $u - w$。它們的和是 \_\_\_\_\_\_\_\_。繪製一個平面三角形的頭尾相接加法，其中包含 $(3, 1)$ 加上 $(-1, 1)$ 加上 $(-2, -2)$。

22 描繪組合 $cu + dv + ew$ 的金字塔，其中 $c \ge 0, d \ge 0, e \ge 0$ 且 $c + d + e \le 1$。標示向量 $\frac{1}{3}(u + v + w)$ 在此金字塔內部或外部。

23 如果您檢視所有 $u, v, w$ 的組合，是否有任何向量無法由 $cu + dv + ew$ 產生？如果 $u, v, w$ 都位於 \_\_\_\_\_\_\_\_ 中，答案會有所不同。

24 哪些向量是 $u$ 和 $v$ 的組合，同時也是 $v$ 和 $w$ 的組合？

25 繪製向量 $u, v, w$，使其組合 $cu + dv + ew$ 只填滿一條線。找到向量 $u, v, w$，使其組合 $cu + dv + ew$ 只填滿一個平面。

26 哪種組合 $c \begin{bmatrix} 1 \\ 2 \end{bmatrix} + d \begin{bmatrix} 3 \\ 1 \end{bmatrix}$ 會產生 $\begin{bmatrix} 14 \\ 8 \end{bmatrix}$？將這個問題表示為關於線性組合係數 $c$ 和 $d$ 的兩個方程式。

27 複習問題。在 $xyz$ 空間中，$i = (1, 0, 0)$ 和 $i + j = (1, 1, 0)$ 的所有線性組合構成的平面在哪裡？

挑戰問題

28 找到向量 $v$ 和 $w$，使得 $v + w = (4, 5, 6)$ 和 $v - w = (2, 5, 8)$。這是一個有 \_\_\_\_\_\_\_\_ 個未知數的問題，並且有相同數量的方程式來找到這些數。

29 找到三個向量 $u = (1, 3)$、$v = (2, 7)$ 和 $w = (1, 5)$ 的兩種不同組合來產生 $b = (0, 1)$。一個稍微複雜的問題：如果我在平面上取任意三個向量 $u, v, w$，是否總會有兩種不同的組合來產生 $b = (0, 1)$？

30 $u = (a, b)$ 和 $w = (c, d)$ 的線性組合會填滿平面，除非 \_\_\_\_\_\_\_\_。找到四個向量 $u, v, w, z$，每個向量有四個分量，使其組合 $cu + dv + ew + fz$ 能夠產生四維空間中的所有向量 $(b_1, b_2, b_3, b_4)$。

31 寫出三個關於 $c, d, e$ 的方程式，使得 $cu + dv + ew = b$。您能以某種方式找到 $c, d, e$ 嗎？
$u = \begin{bmatrix} 2 \\ -1 \\ 0 \end{bmatrix}$ $v = \begin{bmatrix} -1 \\ 2 \\ -1 \end{bmatrix}$ $w = \begin{bmatrix} 0 \\ -1 \\ 2 \end{bmatrix}$ $b = \begin{bmatrix} 1 \\ 0 \\ 0 \end{bmatrix}$

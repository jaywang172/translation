## 130 章節 3. 向量空間與子空間

**21** 將行向量 1 加到行向量 2 產生 B。將行向量 1 加到行向量 1 產生 C。
A 的列向量的線性組合是否能產生 B 或 C？
哪一個有相同的列向量？

$$ A = \begin{bmatrix} 1 & 2 \\ 2 & 4 \end{bmatrix} \quad \text{and} \quad B = \begin{bmatrix} 1 & 2 \\ 3 & 6 \end{bmatrix} \quad \text{and} \quad C = \begin{bmatrix} 1 & 3 \\ 2 & 6 \end{bmatrix} $$

**22** 對於哪些向量 $(b_1, b_2, b_3)$，這些系統有解？

$$ \begin{bmatrix} 1 & 1 & 1 \\ 0 & 1 & 1 \\ 0 & 0 & 1 \end{bmatrix} \begin{bmatrix} x_1 \\ x_2 \\ x_3 \end{bmatrix} = \begin{bmatrix} b_1 \\ b_2 \\ b_3 \end{bmatrix} \quad \text{and} \quad \begin{bmatrix} 1 & 1 & 1 \\ 0 & 1 & 1 \\ 0 & 0 & 0 \end{bmatrix} \begin{bmatrix} x_1 \\ x_2 \\ x_3 \end{bmatrix} = \begin{bmatrix} b_1 \\ b_2 \\ b_3 \end{bmatrix} $$

**23** (推薦) 如果我們向矩陣 A 添加一個額外的列向量 b，則列空間會變大。
給出一個例子，其中列空間確實變大，以及一個例子，其中它沒有變大。
為什麼 $Ax = b$ 有解恰好當且僅當 b 在 A 的列空間中？
$[A \ b]$？

**24** AB 的列向量是 A 的列向量的線性組合。這意味著 AB 的列空間包含在（可能等於）A 的列空間中。
給出一個例子，其中 A 和 AB 的列空間不相等。

**25** 假設 $Ax = b$ 和 $Ay = b^*$ 都有解。那麼 $Az = b + b^*$ 有解嗎？
這是什麼意思？這可以轉化為：如果 b 和 b* 在 A 的列空間中，那麼 b + b* 在 C(A) 中。

**26** 如果 A 是一個 5x5 的可逆矩陣，那麼它的列空間是什麼？為什麼？

**27** 真或假（用一個反例來證明它是假的）：

(a) C(A) 中的向量形成一個子空間。
(b) 如果 C(A) 僅包含零向量，那麼 A 是零矩陣。
(c) 如果 C(A) 包含 2A，則 C(A) 等於零空間。
(d) C(A) 的列空間是 $A^T$ 的行空間（測試此項）。

**28** 構造一個 3x3 矩陣，其列空間的維度為 1，且包含 (1, 0, 1) 和 (0, 1, 0)。

**29** 構造一個 3x3 矩陣，其列空間的維度為 1，且不包含 (1, 1, 1)。

**30** 如果 A 是 9x11 矩陣，且 A 的秩為 8，那麼 A 的零空間是什麼？
